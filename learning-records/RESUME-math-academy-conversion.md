# Resume note: Math Academy conversion (session of 2026-09-19)

Nothing below is committed yet. Branch: `fixes`.

## Done
- **20 unconverted lessons** now use gated chunks: 0061, 0070, 0071, 0073–0078, 0083, 0084,
  0110, 0111, 0115–0121. Each has a gate per chunk with at least one intuition question and a
  `chunk-final` check.
- **~40 partly-converted lessons fixed:**
  - Chunks with no gate got one; before the fix they deadlocked the final check.
  - Gates with no intuition question got one added.
  - Missing `hidden` attributes added; final chunks renamed to `chunk-final`.
  - 0082 (Figma) had curly-quote attributes, which broke its markup, plus a wrong answer key. Both fixed.
- **Option-length tells:** 935 questions rebalanced, down to 0 over the 30% spread. Tool:
  `tools/set_options.py`.
- **Answer keys:** a heuristic scan flagged 81 questions; all were reviewed by hand and were
  correct except the one in 0082.
- **`reference/mastery.js` rewritten:**
  - SM-2-lite that ignores early correct reviews.
  - A chunk with no gate no longer blocks the lesson.
  - Lesson completion is recorded in `sd-progress-v1`.
  - "Show every section" to reread a completed lesson.
  - Options are shuffled on each visit.
  - **Commit-first on intuition questions:** write 6+ words before the options appear.
  - Written answers stored in `sd-answers-v1`.
- **`reference/review.html` (new):**
  - Today's forgetting-curve review, cumulative section exams (85% pass), weak spots, and an interleaved drill.
  - A "Report for your teacher" that includes the learner's own words on missed intuition questions.
  - Commit-first on intuition questions.
- **`reference/question-bank.js`** is generated by `tools/build_question_bank.py`.
  `drill.html` now redirects to `review.html#drill`; the glossary and mock-interview links were updated.
- **Diagnostic (0000):** feeds the review schedule, stores its result in `sd-diagnostic-v1`, and
  shows a copyable report for the chat.
- **`index.html`:** a "Start every session here" banner showing how many questions are due, plus review links.
- **`CLAUDE.md` (new):** the teaching protocol for chat sessions (review first, diagnose, small
  chunks, cumulative section quiz, ledger).
- **`learning-records/review-schedule.md` (new):** the teacher's ledger. It's empty because the
  learner hasn't taken the diagnostic yet.
- **Tools:** `tools/audit_lessons.py` (last run: 0 issues), `convert_lesson.py`, `add_gate.py`,
  `set_options.py`, `build_question_bank.py`.
- **Headless tests (jsdom, in the session scratchpad, not in the repo):**
  - All 124 gated lessons unlock end to end, including the commit-first flow.
  - Diagnostic, index banner, and review today/exam/drill/report all worked.

## Finished in the follow-up session (2026-09-19, later)
1. Bank rebuilt (2,152 questions) and audit rerun: 0 issues.
2. Report verified: missed intuition items show a `my own words: "..."` line.
3. `lessons/_AUTHORING-GUIDE.md` now documents shuffled options, commit-first intuition
   questions, length tells, and the required audit + bank rebuild after edits.
4. `NOTES.md` updated: teaching approach, a "Math Academy conversion" section, drill → review.
5. Visual check via headless Chrome screenshots (0061, review.html, index.html): all render correctly;
   the commit-first box works. `index.html` pills relabelled Done → Ready (they meant "authored",
   and read as "completed").

## Still to do
6. Commit when the learner approves.
7. Residual cue: the correct option is still the uniquely longest about 51% of the time (chance
   is about 31%), all within the 30% spread. Commit-first covers intuition questions. Tightening
   recall-question options to within 15% is optional follow-up work.
